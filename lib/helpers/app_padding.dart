import 'package:flutter/cupertino.dart';

class AppPadding {
  static const horizPadding8 = EdgeInsets.symmetric(horizontal: 8);
  static const horizPadding5 = EdgeInsets.symmetric(horizontal: 5);
  static const horizPadding10 = EdgeInsets.symmetric(horizontal: 10);
  static const horizPadding15 = EdgeInsets.symmetric(horizontal: 15);
  static const allPadding8 = EdgeInsets.all(8);
  static const bottomOnly8 = EdgeInsets.only(bottom: 8);
  static const bottomOnly20 = EdgeInsets.only(bottom: 20);
}
